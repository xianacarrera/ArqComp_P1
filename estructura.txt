# Estructura del código

Cada experimento está determinado por 2 parámetros:
* L, que toma valores 0.5*S1, 1.5*S1, 0.5*S2, 0.75*S2, 2*S2, 4*S2, 8*S2 
* D, que toma 5 valores entre 1 y 100: <COMPLETAR AQUÍ>

donde 
* S1 = número de líneas caché que caben en la caché L1 de datos
* S2 = número de líneas caché que caben en la caché L2


El script bash_script.sh permite automatizar todas las pruebas. Desde él:

* Para cada L
    * Para cada D
        * Se realizan 10 medidas del tiempo en ciclos de reloj ejecutando el programa .c
        * Se toma la mediana de esas 10 medidas y se escribe en resultados.txt
        
(7 * 5 * 10 = 350 medidas; 35 resultados)

Dentro del programa .c, la operación de reducción sobre el vector A[] se repite 10 veces,
pero esos resultados son iguales entre sí. El tiempo en ciclos por acceso a memoria se 
